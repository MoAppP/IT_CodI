<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.it_codi">

    <uses-permission android:name="android.permission.GET_TASKS"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/it_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/it_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.IT_CodI"
        android:usesCleartextTraffic="true" >
        <activity
            android:name=".activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.LoadingActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen" />
        <activity android:name=".activity.ClothesinfoActivity"
            android:theme="@android:style/Theme.Dialog" />
        <activity android:name=".activity.SetupPopupActivity" android:theme="@android:style/Theme.Dialog"/>
        <activity android:name=".activity.ListLoadingActivity"
            android:theme="@style/Theme.AppCompat.Transparent.NoActionBar"/>
    </application>

</manifest>